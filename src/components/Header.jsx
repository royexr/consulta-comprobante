// Dependencies
import React from 'react';

// Resources
import './Header.css';

const Header = () => (
  <>
    <header className="header">
      <nav>
        <a className="logo" href="/">LOGO</a>
      </nav>
    </header>
  </>
);

export default Header;
